import 'package:flutter/material.dart';

class EqualizerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Equalizer'),
      ),
      body: Center(
        child: Text('Equalizer Screen'),
      ),
    );
  }
}